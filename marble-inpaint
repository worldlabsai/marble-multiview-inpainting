#!/usr/bin/env sh
set -eu

if ! command -v uv >/dev/null 2>&1; then
  echo "marble-inpaint needs uv: https://docs.astral.sh/uv/getting-started/installation/" >&2
  exit 127
fi

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec uv run --project "$repo_dir" --frozen marble-inpaint "$@"
